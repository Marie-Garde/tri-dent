{
  "name": "tri-dent",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "clean": "npx nuxi cleanup",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "NUXT_APP_BASE_URL=/tri-dent/ npx nuxt build --preset github_pages && npx gh-pages --dotfiles -d .output/public"
  },
  "dependencies": {
    "@nuxt/fonts": "^0.12.1",
    "nuxt": "^4.2.0",
    "vue": "^3.5.22",
    "vue-router": "^4.6.3"
  },
  "devDependencies": {
    "@nuxt/icon": "^2.1.0",
    "sass-embedded": "^1.93.2"
  }
}
